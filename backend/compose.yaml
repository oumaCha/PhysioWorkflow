services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: physioflow
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "5433:5432"
    volumes:
      - physio_pgdata:/var/lib/postgresql/data

volumes:
  physio_pgdata:
